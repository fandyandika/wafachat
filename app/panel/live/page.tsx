import { LiveTodayDashboard } from '@/components/panel/live-today-dashboard';

// Owner "Live Hari Ini" — opened on demand; renders the calendar-day live view.
export default function Page() {
  return <LiveTodayDashboard />;
}
